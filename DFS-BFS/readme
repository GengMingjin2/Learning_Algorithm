图：
    结点 + 边

存储一个图：
1、邻接矩阵：一个N*N的二维数组E，用E[i][j]表示结点i和j之间是否相连，1表示有边，0表示没有；
（效率低：当节点数量过多时，无论是时间还是空间其复杂度都很高，同时当存在复数边时，无法表示）
c++:
(int E[110][110];)
(vector<vector<int> > E(110);)
python:
a = [[0, 0],[0, 1]]

2、邻接链表：使用一个链表的方式保存一个结点的所有边（每个结点都有一个链表）
(vector<int> E[110]; //一个数组E，其中E的每个元素是一个vector！！
E[3].push_back(6) // 有一条从3到6的边)

3、前向星：？

搜索的数据结构：

BFS：队列
DFS：栈
