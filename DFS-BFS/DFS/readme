DFS: Deep-First-Search 宽度优先遍历

本质是一种搜索方法（对于一个图或树来说，从某个点开始，搜索与其相连的结点，一直到搜索尽头为止，然后以此连接下去，直到所有的点都被搜索到。）

DFS的实现需要一个栈辅助保存（因为每次找到和u相连的点后要继续向下搜索，符合先进后出）
（栈的特性：先进后出，即push时从栈顶进入，pop时从栈顶弹出！！！）

基本逻辑如下：

需要一个与图同等大小的数组来保存访问状态，保证不会重复访问！


